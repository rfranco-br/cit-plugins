# CI&T Plugins Registry

This repository hosts CI&T internal plugins for Claude Code, distributed via the Centaurus marketplace.

Each plugin lives on its own branch: `plugin/<plugin-name>`.

To browse available plugins, visit https://centaurusdirectory.web.app
